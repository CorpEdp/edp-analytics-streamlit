# requirements.txt — STARTER FILE
#
# This is a reasonable guess based on common libraries used in ERP / payment /
# reconciliation / reporting Streamlit apps (uploads, Excel, dates, charts, APIs).
# It is NOT auto-generated from your real code yet.
#
# Once you paste your real 21 app.py files into modules/, generate the
# accurate version by running, from the project root, in a clean venv:
#
#   pip install pipreqs
#   pipreqs . --force
#
# That will scan every import statement across all 21 modules and produce
# an exact, no-bloat requirements.txt. Then you can delete/replace this file.

streamlit==1.63.0
pandas==3.0.5
numpy==2.5.3
matplotlib==3.10.8
openpyxl==3.1.5          # reading/writing .xlsx
xlsxwriter==3.2.9       # writing formatted Excel reports
python-dateutil==2.9.0.post0
requests==2.32.5        # for Cashfree / payment gateway API calls
plotly==7.0.0            # charts/analytics
gspread==6.2.1           # if any module reads/writes Google Sheets
google-auth==2.47.0      # needed alongside gspread
Pillow==12.1.1           # if any module handles images
